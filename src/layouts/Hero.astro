<!-- HERO-SEKTION -->
<section class="hero">
  <video autoplay muted loop playsinline preload="auto" onloadeddata="this.currentTime=0;">
    <source src="src/assets/Toki.mp4" type="video/mp4" />
    Din browser understøtter ikke video-tagget.
  </video>
</section>

<style>
  /* Desktop hero */
  .hero video {
    position: absolute;
    width: 100%;
    height: 90vh;
    object-fit: cover;
    top: 0;
    left: 0;
    z-index: 0;
  }

  /* Mobile adjustments */
  @media (max-width: 768px) {
    .hero video {
      position: relative; /* gør den en del af flowet */
      width: 100%; /* fylder nu hele mobilbredden */
      height: 40vh; /* mindre højde på mobil */
      object-fit: cover;
      top: 0;
      left: 0;
      z-index: 0;
    }

    .hero {
      position: relative;
      margin-top: 0; /* så den ikke overlapper header/topbar */
    }
  }
</style>
